function findPair(arr) {
    sum = 0;

    for(let i = 0; i < arr.length - 1; i++) {
        for (let j = i + 1; j < arr.length; j++) {

        }
    }
}

let arr = [8, 7, 2, 5, 3, 1];
let sum = 10;

findPair(arr, sum);

// Farmácia, Armazém e uma Biblioteca